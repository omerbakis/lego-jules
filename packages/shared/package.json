{
  "name": "@lego/shared",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "echo 'no lint for shared'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no test for shared'"
  },
  "devDependencies": {
    "@lego/config": "workspace:^",
    "typescript": "^5.5.0"
  }
}
